/**
 * @magnitudedev/icn-contracts
 *
 * Effect Schema mirrors of inference/crates/icn-contracts.
 * Wire OpenAPI types remain authored in Rust and generated into
 * @magnitudedev/icn-protocol; this package owns the engine-facing contracts
 * (execution intent, inference request/result types, inventory helpers,
 * bootstrap records, streaming output helpers) used by the TypeScript engine.
 *
 * Public surface is filled in by the wave-2 contracts agent.
 */
export {}
